import React from 'react';

function QuestionForm() {
  // render form for answering a question
  // on submit, post form data back to the api
  // show message that the data has been submitted/recieved
};

export default QuestionForm;